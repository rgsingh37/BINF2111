BINF2111 Lecture 10 

count steven : egrep -o 'Steven.Jose' file.tsv | wc-1

sed 's/ /,/g' file.txt | more (convert to csv) then work from there 

how do I extract the sequence with grep:

grep 'ATG.*ATG'file.fna


fastq - nucleotide file 

amino acids=(

for amino acids

cat -n doppleganger_names.txt 

grab lines and change color

cat- n doppelganger_names.txt | grep "david" color

how do i grab line one only?
sed -n '1p' doppelganger_names.txt
or 
head -1 doppelganger_names.txt | tail +1

how do i grab line one and two?
sed -n '1p;2p' doppelganger_names.txt 
or
head -2 doppelganger_names.txt | tail +1

how do i print number 1 only and grab david with color?
cat -n doppelganger_names.txt | sed -n '1p' | grep "david" --color

print a range of lines 
sed -n '3,7p' doppelganger_names.txt 
head -7 doppelganger_names.txt | tail +3

sed -n '3p;7p' file.tsv

use semi colon to only get line 3,7 not regular comma 

num1=$1 
num2=$2

if [ $num1 -eq $num2]; then 
	echo "the number match"
else
	echo"the numbder dont match"
fi

if equal == 
not equal !=
less than -le 
greater than -ge

bash 
read
Hello World! [enter]
echo $REPLY 

bash for loop : for i in file.*;do command $i done 
bash for loop :for i in {1..5}
do 
  echo "Welcome $i times" 
done 

bash c style: for ((c=1;  c<=5; c++))
do 
  echo "Welcome $i times"
done

python:
for x in file:
command 
